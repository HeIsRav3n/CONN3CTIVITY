/**
 * @deprecated Browser Neon SQL removed — use src/lib/api.js (Vercel /api/*).
 * Kept as a stub so accidental imports fail loudly in development.
 */
export function sql() {
  throw new Error('Direct Neon SQL from the browser is disabled. Use fetchStats / fetchConn3ctors / fetchMvc from src/lib/api.js')
}
